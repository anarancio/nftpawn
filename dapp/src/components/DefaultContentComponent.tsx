import { FC } from 'react';

const DefaultContentComponent: FC = () => {
    return (
        <div style={{fontSize: 'xx-large', textAlign: 'center', paddingTop: '50px'}}>
            In a future version here we can add protocol’s and user‘s stats
        </div>
    );
};

export default DefaultContentComponent;